apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  # Pre-created independently of Helm so that `helm uninstall` never deletes this PVC.
  # Mounted at /prometheus via prometheusSpec.volumes + volumeMounts in values.yaml.
  name: prometheus-data
  namespace: monitoring
  labels:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/component: storage
    app.kubernetes.io/managed-by: kubectl
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 20Gi
  # Uncomment and set volumeName when rebinding to a Released PV after accidental deletion:
  # volumeName: <pv-name>
